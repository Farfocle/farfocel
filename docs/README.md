# Docs

Doxygen HTML documentation for the engine. Built as the `docs` CMake target (requires Doxygen).

```sh
cmake -B build
cmake --build build --target docs
```

Output goes to `build/docs/doxygen/`.
